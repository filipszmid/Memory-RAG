"""
Deduplication sub-package — Task 2 stubs.

Future implementation:
- Semantic similarity-based deduplication (embeddings + cosine similarity)
- Fact-key conflict resolution with soft-delete (superseded_by chain)
- Two-stage pipeline: Jaccard prefilter → embedding comparison
"""
