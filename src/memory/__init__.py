"""
Memory sub-package — Task 3 stubs.

Future implementation:
- ElasticSearch as unified vector + key-value store for user facts
- Hybrid injection strategy: full prompt injection (<20 facts) + RAG (>=20 facts)
- Adaptive per-turn session injector with already_injected_fact_ids tracking
"""
